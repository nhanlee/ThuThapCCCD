services:
  - type: web
    name: ThuThapCCCD
    env: python
    plan: free
    buildCommand: pip install -r requirements-simple.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16